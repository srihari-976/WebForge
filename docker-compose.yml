services:
  webforge:
    build: .
    volumes:
      - ./sandbox:/app/sandbox
    environment:
      - OLLAMA_HOST=http://ollama:11434
      - OLLAMA_TIMEOUT=180
      - WEBBUILDER_USE_CREWAI=false
    depends_on:
      - ollama
    ports:
      - "8000:8000"

  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

volumes:
  ollama_data:
